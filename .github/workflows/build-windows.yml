name: Build Windows exe

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Publish a release with this tag (e.g. v1.0.12-eg1). Leave empty to just build.'
        required: false
        default: ''
  # push trigger so the build runs from this feature branch —
  # workflow_dispatch only works once the file is on the default branch
  push:
    branches:
      - 'claude/**'
    # version tags (v1.0.12-eg1 etc.) build the exe AND publish a GitHub release
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Build Windows executable
        run: npm run build:win

      - name: Bundle ffmpeg
        shell: pwsh
        run: |
          # Static Windows ffmpeg from BtbN (GitHub-hosted, reliable for CI).
          # The app spawns `ffmpeg`, so ship it alongside the exe like the
          # upstream releases do.
          Invoke-WebRequest -Uri "https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip" -OutFile ffmpeg.zip
          # log the hash of what was actually shipped — the 'latest' release
          # has no stable checksum to pin against, so at least make each
          # build's ffmpeg traceable from the CI log
          Get-FileHash ffmpeg.zip -Algorithm SHA256 | Format-List
          Expand-Archive -Path ffmpeg.zip -DestinationPath ffmpeg_extract -Force
          $ff = Get-ChildItem -Path ffmpeg_extract -Recurse -Filter ffmpeg.exe | Select-Object -First 1
          if (-not $ff) { throw "ffmpeg.exe not found in download" }
          Copy-Item $ff.FullName -Destination ffmpeg.exe -Force

      - name: Package release zip
        shell: pwsh
        run: Compress-Archive -Path tablo2plex-win-x64.exe, ffmpeg.exe -DestinationPath tablo2plex-win-x64.zip -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tablo2plex-win-x64
          path: tablo2plex-win-x64.zip
          if-no-files-found: error
          retention-days: 30

      - name: Create release
        if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.release_tag != '')
        uses: softprops/action-gh-release@v2
        with:
          # on tag pushes the tag already exists; on manual dispatch the
          # action creates the tag at the built commit
          tag_name: ${{ inputs.release_tag || github.ref_name }}
          # ship both the self-contained zip (exe + ffmpeg) and the bare exe
          files: |
            tablo2plex-win-x64.zip
            tablo2plex-win-x64.exe
          generate_release_notes: true
